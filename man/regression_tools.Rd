% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign.R, R/io.R, R/regression-tools.R,
%   R/retrieve.R
\name{assign_outliers}
\alias{assign_outliers}
\alias{assign_td}
\alias{export_outliers}
\alias{import_outliers}
\alias{export_td}
\alias{import_td}
\alias{regression_tools}
\alias{retrieve_outliers}
\alias{retrieve_td}
\title{Manage regression components in JDemetra+ workspaces}
\usage{
assign_outliers(jws, outliers, verbose = TRUE)

assign_td(jws, td, verbose = TRUE)

export_outliers(outliers, path = NULL, verbose = TRUE)

import_outliers(path, verbose = TRUE)

export_td(td, path = NULL, verbose = TRUE)

import_td(path, verbose = TRUE)

retrieve_outliers(
  jws,
  domain = TRUE,
  estimation = FALSE,
  point = FALSE,
  verbose = TRUE
)

retrieve_td(
  jws,
  domain = TRUE,
  estimation = FALSE,
  point = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{jws}{A Java Workspace object, as returned by
\code{\link[rjd3workspace:jws_open]{rjd3workspace::jws_open()}} or \code{\link[rjd3workspace:jws_new]{rjd3workspace::jws_new()}}.}

\item{outliers}{[\link[base]{data.frame}] A data.frame created with
\link{retrieve_outliers} or \link{import_outliers}. See Format section for more
information about the format of this argument.}

\item{verbose}{Boolean. Print additional informations. Default is \code{TRUE}.}

\item{td}{[\link[base]{data.frame}] A data.frame created by \link{retrieve_td}
or \link{import_td}. See Format section for more information about the format
of this argument.}

\item{path}{\link{character} Path to a YAML file to read or write a table.}

\item{domain}{Boolean indicating if outliers should be extracted from
the domain specification.}

\item{estimation}{Boolean indicating if outliers should be extracted
from the estimation specification.}

\item{point}{Boolean indicating if outliers should be extracted
from the point specification.}
}
\value{
\itemize{
\item \code{retrieve_outliers()} and \code{import_outliers()} returns data.frame
representing the outliers.
\item \code{retrieve_td()} and \code{import_td()} returns data.frame representing the
trading days variables
\item \code{export_outliers()} and \code{export_td()} functions invisibly return the path
of the YAML file written.
\item \code{assign_XXX()} functions invisibly return the updated workspace \code{jws}.
}
}
\description{
These functions allow extracting, exporting, importing and assigning
regression components used in JDemetra+ workspaces.
}
\details{
Two types of regression components are currently supported:
\itemize{
\item \strong{Outliers}
\item \strong{Trading-day regressors (TD)}
}
}
\section{Format}{


\subsection{Outliers table}{

Outliers are represented by a \code{data.frame} with \strong{three columns}:
\itemize{
\item \code{series} : name of the series in the workspace.
\item \code{type} : type of outlier (\code{AO}, \code{LS}, \code{TC} or \code{SO}).
\item \code{date} : date of the outlier in \code{YYYY-MM-DD} format.
}

These tables are typically created with \code{\link[=retrieve_outliers]{retrieve_outliers()}} or
\code{\link[=import_outliers]{import_outliers()}}.

}

\subsection{Trading-day table}{

Trading-day specifications are represented by a \code{data.frame}
with \strong{two columns}:
\itemize{
\item \code{series} : name of the series in the workspace.
\item \code{regs} : name of the trading-day regressor set to apply
(e.g. \code{REG1}, \code{REG2}, ..., optionally with \code{LY}).
}

These tables are typically created with \code{\link[=retrieve_td]{retrieve_td()}} or
\code{\link[=import_td]{import_td()}}.

}
}

\section{Workflow}{


The workflow typically follows these steps:
\enumerate{
\item Extract regression information from a workspace (\code{retrieve_XXX()})
\item Optionally export it to a YAML file (\code{export_XXX()})
\item Import it later from the YAML file (\code{import_XXX()})
\item Assign the regression specification to another workspace (\code{assign_XXX()})
}
}

\section{Other}{


The assignment functions (\code{assign_XXX()}) modify the \strong{first SA-Processing}
of the workspace.

Currently, regression information can be extracted (\code{retrieve_XXX()}) from
the point, estimation or domainSpec, while the assignment step
(\code{assign_XXX()}) is performed in both the domainSpec and the estimationSpec.
}

\examples{
\dontshow{if (rjd3toolkit::get_java_version() >= rjd3toolkit::minimal_java_version) withAutoprint(\{ # examplesIf}

library("rjd3workspace")
library("rjd3toolkit")
\donttest{
my_data <- ABS[, 1:3]
jws <- create_ws_from_data(my_data)
set_context(jws, create_insee_context(start = c(2015L, 1L)))

## Outliers

# Read all the outliers from a workspace
outs <- retrieve_outliers(jws, point = TRUE, domain = FALSE)

# Export outliers
path_outs <- tempfile(pattern = "outliers-table", fileext = ".yaml")
export_outliers(outs, path_outs)

# Import outliers from a file
outs2 <- import_outliers(path_outs)

# Assign the outliers to a WS
assign_outliers(jws = jws, outliers = outs2)


## Trading day workflow

# Read all the td variables from a workspace
td <- retrieve_td(jws)

# Export td variables
path_td <- tempfile(pattern = "td-table", fileext = ".yaml")
export_td(td, path_td)

# Import td variable from a file
td2 <- import_td(path_td)

# Select td
td3 <- select_td(my_data)

# Assign the td variables to a WS
assign_td(jws = jws, td = td3)
}
\dontshow{\}) # examplesIf}
}
\concept{regression tools}
