% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modify-sao.R
\name{add_raw_data_path}
\alias{add_raw_data_path}
\title{Add raw data from a file to a JWS workspace}
\usage{
add_raw_data_path(jws, path, ...)
}
\arguments{
\item{jws}{A Java Workspace object, as returned by
\code{\link[rjd3workspace:jws_open]{rjd3workspace::jws_open()}} or \code{\link[rjd3workspace:jws_new]{rjd3workspace::jws_new()}}.}

\item{path}{A character string. Path to the input data file. Must be a
\code{.csv} file (support for \code{.xlsx} is not yet implemented).}

\item{...}{Addional arguments passed to
\code{rjd3providers::txt_data()} (e.g., delimiter, date format, clean
missing argument...).}
}
\value{
The modified \code{jws} object invisibly.
}
\description{
This function completes the ts metadata (moniker) to make the workspace
refreshable and crunchable.
}
\details{
Currently, only CSV files are supported. Each column of the input file is
interpreted as a time series and matched against the series names in the
workspace.

The difference with the function \code{\link{make_ws_crunchable}} is that
\code{add_raw_data_path()} will associate the workspace with a non temporary data
path.
}
\examples{
\dontshow{if (rjd3toolkit::get_java_version() >= rjd3toolkit::minimal_java_version) withAutoprint(\{ # examplesIf}

library("rjd3workspace")
library("rjd3x13")
library("rjd3toolkit")

my_data <- ABS
path_ABS <- system.file("extdata", "ABS.csv", package = "rjd3providers")
\donttest{
jws <- create_ws_from_data(my_data)
add_raw_data_path(jws, path_ABS, delimiter = "COMMA")
}
\dontshow{\}) # examplesIf}
}
